{% extends 'base.html.twig' %}


{% block body %}
    
    <h2>{{ event.name}}</h2>
        
    <div class="metadata">Publié le : {{ event.date |date('d-m-Y')}}</div>
        
    <div class="content">
        {{ event.description}}
    </div>
   
    

  
{% endblock %}