{% extends 'base.html.twig' %}


{% block body %}
    {% for event in events %}
        <a href="{{path('show_info_event', { 'id': event.id })}}" ><h2>{{ event.name}}</h2></a>
        
        <div class="metadata">Publié le : {{ event.date |date('d-m-Y')}}</div>
        
        <div class="content">
            {{ event.description}}
        <a href="{{path('deleteEvent', { 'id': event.id })}}" ><h2>Delete</h2></a>
        <a href="{{path('EventUpdate', { 'id': event.id })}}" ><h2>Update</h2></a>
        </div>
    {% endfor %}
    

  
{% endblock %}